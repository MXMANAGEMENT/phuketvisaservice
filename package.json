{
  "name": "phuketvisaservice",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "verify": "node scripts/verify.mjs .",
    "build": "node scripts/build.mjs",
    "verify:dist": "node scripts/verify.mjs dist",
    "check": "npm run verify && npm run build && npm run verify:dist"
  }
}
