# Cloudflare Pages — redirects
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/
# First match wins. Limit: 2100 static + 100 dynamic rules.

# Security.txt redirect
/security.txt    /.well-known/security.txt    301

# IMPORTANT:
# - Do NOT redirect to visaservicephuket.com. This stays a standalone landing page.
# - HTTP -> HTTPS for the apex is handled automatically by Cloudflare ("Always Use HTTPS").
# - Force www -> non-www should be handled in Cloudflare Dashboard (Custom Domains).
# - Language directories (/de/, /ru/) are served as-is; no redirects needed.
